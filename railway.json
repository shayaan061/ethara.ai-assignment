{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "cd backend && python3 -m ensurepip && python3 -m pip install -r requirements.txt"
    },
    "deploy": {
        "startCommand": "cd backend && gunicorn config.wsgi --bind 0.0.0.0:$PORT"
    }
}